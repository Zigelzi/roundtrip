# Runtime SQLite database (incl. WAL sidecar files)
*.db
*.db-shm
*.db-wal

# Build output
/build/

# Local environment overrides
.env

# Built Tailwind bundle (generated by `make generate` from cmd/web/tailwind.css)
/cmd/web/static/tailwind.css

# make dev: air's build output, and templ's watch-mode text files
/tmp/
*_templ.txt
