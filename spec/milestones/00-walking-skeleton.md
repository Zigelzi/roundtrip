# Milestone 00: Walking skeleton

**Status:** draft · **Branch:** chore/walking-skeleton
<!-- Status: draft → red-teamed → in-progress → done -->

## Why
<!-- Prove the whole stack integrates and runs locally before any feature is built.
     Serves no user flow directly — it de-risks the stack and tooling. -->

## Scope
- **In:** one HTTP route → renders one templ page → from one SQLite query (via sqlc), styled with the standalone Tailwind CLI. Goose applies the initial schema.
- **Out:** any real feature or user flow; authentication; Pi deployment (deferred to its own later milestone).

## Acceptance conditions (BDD)
<!-- These become the `go test` cases, written before implementation. -->
- ...

## Open questions
- ...
