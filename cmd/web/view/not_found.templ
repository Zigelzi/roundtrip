package view

templ NotFound(message string) {
	@Layout(message) {
		<h1 class="text-2xl font-bold">{ message }</h1>
		<a href="/" class="mt-4 flex min-h-12 items-center justify-center rounded-lg bg-slate-900 px-4 font-semibold text-white">Back to trips</a>
	}
}
